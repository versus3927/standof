cmake_minimum_required(VERSION 3.10.2)
project("femboy_strike")

add_library(femboy_strike SHARED
            main.cpp
            memory.cpp
            esp.cpp
            menu.cpp)

find_library(log-lib log)
target_link_libraries(femboy_strike ${log-lib})
